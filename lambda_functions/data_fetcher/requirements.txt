nflreadpy>=0.1.0
pandas>=2.0.0,<2.3.0
numpy>=1.24.0,<2.0.0
# boto3 provided by Lambda runtime
# polars and pandas required by nflreadpy for different data types
# Using numpy <2.0 and pandas <2.3 to avoid compilation requirements
